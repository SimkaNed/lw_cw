"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class plant_states_t(object):
    __slots__ = ["timestamp", "mode", "measurements", "error_code"]

    __typenames__ = ["int64_t", "int16_t", "double", "int32_t"]

    __dimensions__ = [None, None, [1], None]

    def __init__(self):
        self.timestamp = 0
        self.mode = 0
        self.measurements = [ 0.0 for dim0 in range(1) ]
        self.error_code = 0

    def encode(self):
        buf = BytesIO()
        buf.write(plant_states_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qh", self.timestamp, self.mode))
        buf.write(struct.pack('>1d', *self.measurements[:1]))
        buf.write(struct.pack(">i", self.error_code))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != plant_states_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return plant_states_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = plant_states_t()
        self.timestamp, self.mode = struct.unpack(">qh", buf.read(10))
        self.measurements = struct.unpack('>1d', buf.read(8))
        self.error_code = struct.unpack(">i", buf.read(4))[0]
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if plant_states_t in parents: return 0
        tmphash = (0xa694a3e8aff1dac1) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if plant_states_t._packed_fingerprint is None:
            plant_states_t._packed_fingerprint = struct.pack(">Q", plant_states_t._get_hash_recursive([]))
        return plant_states_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", plant_states_t._get_packed_fingerprint())[0]

